Mac App Play
============

A CLI tool for the agent to use macOS app,
made by Claude Code for themselves.

Usage
-----

`make` to produce `mac_app_play` binary in `Skills/mac-app-play/scripts`, then
place `mac-app-play` skill in your skills directory for the agent.
